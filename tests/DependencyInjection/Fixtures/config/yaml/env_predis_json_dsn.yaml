parameters:
    env(REDIS_DSNS): '["redis://localhost/1","redis://localhost/2"]'

snc_redis:
    clients:
        default:
            type: predis
            alias: default
            dsn: "%env(json:REDIS_DSNS)%"
